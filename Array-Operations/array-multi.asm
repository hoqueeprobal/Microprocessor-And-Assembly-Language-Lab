.MODEL SMALL
.STACK 100H
.DATA
A   DB "1","2","2","2","1"
MULL DB ?

.CODE
MAIN PROC
    MOV AX, @DATA
    MOV DS, AX

    MOV SI, 0
    MOV CX, 5
    MOV AL, 1

LOOPS:
    MOV BL, A[SI]
    SUB BL, 30H
    MUL BL
    INC SI
    LOOP LOOPS

    ADD AL, 30H
    MOV MULL, AL

    MOV DL, MULL
    MOV AH, 2
    INT 21H

    MOV AH, 4CH
    INT 21H
    
MAIN ENDP
END MAIN
