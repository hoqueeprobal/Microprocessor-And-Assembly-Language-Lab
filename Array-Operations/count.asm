.MODEL SMALL
.STACK 100H

.DATA
A DB 3,1,4,2,5
LEN EQU 5
COUNT DB ?

.CODE
MAIN PROC
    MOV AX, @DATA
    MOV DS, AX

    MOV CX, LEN
    MOV COUNT, CL

    MOV AL, COUNT
    ADD AL, 30H
    MOV DL, AL
    MOV AH, 2
    INT 21H

    MOV AH, 4CH
    INT 21H
MAIN ENDP
END MAIN
